version: '3.4'

services:
  postgres:
    image: postgres:10.5
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
  payment_gateway:
    build:
      context: .
      dockerfile: services/payment-gateway/Dockerfile
    env_file:
      - 'dev.env'
    ports:
      - "8080:8080"